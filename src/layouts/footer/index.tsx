import React, { useEffect, useState } from 'react';
import css from './index.less';

const Footer = (props: any) => {
  useEffect(() => {
    return () => {};
  }, []);

  return <div className={css.main}>2022 江苏国光-公用事业部</div>;
};

export default Footer;
